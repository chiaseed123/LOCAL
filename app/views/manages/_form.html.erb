<%= form_with(model: manage, local: true) do |form| %>
  <% if manage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manage.errors.count, "error") %> prohibited this manage from being saved:</h2>

      <ul>
        <% manage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :deadline %>
    <%= form.datetime_select :deadline %>
  </div>

  <div class="field">
    <%= form.label :review_ungraded %>
    <%= form.check_box :review_ungraded %>
  </div>

  <div class="field">
    <%= form.label :update_graded %>
    <%= form.check_box :update_graded %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
